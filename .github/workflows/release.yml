name: Release

on:
    push:
        tags:
            - "v*.*.*"

jobs:
    build-release:
        name: Build / ${{ matrix.os }}
        runs-on: ${{ matrix.os }}

        strategy:
            fail-fast: false
            matrix:
                include:
                    - os: ubuntu-latest
                      artifact_name: incrify-linux-x64
                      binary_path: build/incrify

                    - os: windows-latest
                      artifact_name: incrify-windows-x64
                      binary_path: build/incrify.exe

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Install CMake (Windows)
              if: runner.os == 'Windows'
              run: |
                  choco install mingw --yes
                  choco install cmake --yes --installargs 'ADD_CMAKE_TO_PATH=System'

            - name: Setup CMake (Release)
              run: >
                  cmake -S . -B build
                  ${{ matrix.os == 'windows-latest' && '-G "MinGW Makefiles"' || '' }}
                  -DCMAKE_BUILD_TYPE=Release
                  -DBUILD_TESTS=ON

            - name: Build
              run: cmake --build build --config Release

            - name: Run Tests
              run: ctest --test-dir build -C Release --output-on-failure

            - name: Copy Binary
              shell: bash
              run: |
                  mkdir -p dist
                  cp ${{ matrix.binary_path }} dist/${{ matrix.artifact_name }}

            - name: Upload Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: ${{ matrix.artifact_name }}
                  path: dist/${{ matrix.artifact_name }}

    publish-release:
        name: Publish GitHub Release
        runs-on: ubuntu-latest
        needs: build-release

        steps:
            - name: Download All Artifacts
              uses: actions/download-artifact@v4
              with:
                  path: dist

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  name: Release ${{ github.ref_name }}
                  generate_release_notes: true
                  files: dist/**/*
